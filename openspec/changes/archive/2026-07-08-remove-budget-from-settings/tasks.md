## 1. Remove Budget Section from SettingsView

- [x] 1.1 In `SettingsView`, remove the `Section("Budget")` block and its contents (BudgetProgressBar, utilization let binding, status label, percentage text)
